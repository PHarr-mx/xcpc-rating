# 路线图

> 定位：**计划**（进度与剩余工作的权威来源）。关联：[07-开发流程](07-开发流程.md) · 各模块文档
> 本文档合并了原「13-实施路线图」「14-Web开发拆分计划」与根目录功能缺失评估（GAP_REPORT_PLAN，已删除、内容并入本文）。

---

## 1. 里程碑总览

| 期 | 内容 | 结束标志 | 状态 |
|----|------|----------|------|
| **一期 · 地基** | pyproject 打包、建表、JSON 迁移、contest/rating 骨架、board 聚合、Reflex 骨架、`/` 榜单页 | 能用真实数据看到榜单 | ✅ 2026-08-18 |
| **二期 · 认证** | reflex-local-auth、角色与绑定审批、`/profile`、审计日志 | 权限三处校验点立稳 + 完整绑定审批链路 | ✅ 2026-08-20 |
| **三期 · 管理后台** | 选手/队伍 CRUD、xlsx 在线导入五步（P4a–P4d） | 不再需要敲 CLI 导入 | ✅ 2026-09-10（浏览器手工验收通过） |
| **四期 · 业务补齐** | Rating 正式公式、训练赛录入、权重试算页、图表 | 榜单数值有业务含义 | ⬜ 未开始 |
| **五期 · 上线** | systemd + Caddy + 备份 cron | 公网可访问 | ⬜ 未开始 |

**顺序约束**：一期最先；二期先于三期。四期第一步是**业务定案**（公式与分摊规则），不是写代码；五期进入前须先定注册限制。

进入/退出条件：四期进入前三期完成；五期进入前四期完成。

---

## 2. Web 开发拆分（P0–P6）

| Part | 内容 | 状态 |
|------|------|------|
| P0 | 工程地基（依赖、脚手架、包结构） | ✅ 2026-08-18 |
| P1 | 榜单只读页 `/`（含 URL query 同步 ✅ 2026-09-11） | ✅ |
| P2 | 认证底座（登录注册 + AuthState + 权限守卫） | ✅ 2026-08-20 |
| P3 | `/profile` 自助资料 + 绑定申请 | ✅ 2026-08-20 |
| P4a | `/admin/players` 选手 CRUD | ✅ 2026-08-24 |
| P4b | `/admin/teams` 队伍 CRUD | ✅ 2026-08-24 |
| P4c | `/admin/contests` + `/admin/audit` | ✅ 2026-08-24 |
| P4d | `/admin/import` 在线导入五步 | ✅ 2026-08-25（验收 2026-09-10） |
| P5 | 详情页与图表（`/about` ✅、`/players/{id}` ✅、`/contests/{id}` ✅ 2026-09-14 + oj_link/rating_chart/standings_table；form_fields 取消） | ✅ 2026-09-14 |
| P6 | 权重试算 `/admin/rating` | ⬜（前置：四期公式） |

---

## 3. 剩余工作（按建议顺序）

### 第一步 · 收尾小项（零依赖）

- ~~榜单筛选同步 URL query~~ ✅ 2026-09-11
- ~~data_version 自动 bump~~ ✅ 2026-09-11（`db/meta.py` + player/contest service 接线 + 7 条回归测试）
- ~~`/about` 静态页~~ ✅ 2026-09-11

### 第二步 · P5 详情页主体 ✅（2026-09-14 完成）

1. ~~`/players/{player_id}`~~ ✅ 2026-09-11：档案 + OJ 外链 + 参赛记录表 + recharts 累计曲线（占位公式数据）+ 绑定本人「编辑我的资料」入口 + 404 兜底；core 侧新增 `player_event_history`（`rating/api.py`，测试注入 `configure_session`）
2. ~~`/contests/{contest_id}`~~ ✅ 2026-09-14：元信息行 + 成绩表按 format 切列（xcpc/oi 两套列）；队员姓名解析（含离队）、打星标记、奖项中文；404 兜底
3. 组件：~~`oj_link` / `rating_chart` / `standings_table`~~ ✅（rating_chart 用内置 recharts，替代原计划 plotly——零新增依赖）；`form_fields` 取消（无消费方，admin 页各有表单模式）
4. 联动：榜单选手名 → 选手详情、选手页比赛名 → 比赛详情 ✅

### 第三步 · 四期启动 = 业务定案（写代码前必须拍板）

1. **Rating 公式定案**：加权 Elo / 队内分摊 / 时间衰减 / 初始分与封顶（详见 [04-Rating与榜单](04-Rating与榜单.md) §2–3）→ 产出公式规格写入文档，升级 `meta.rating_algorithm`
2. **训练赛分摊规则定案**：成绩如何摊到选手

### 第四步 · 四期实现（依赖定案）

1. `BaseRatingCalculator` 体系替换 placeholder_v0（版本号、缓存联动、测试重写）
2. 训练赛录入（导入入口 + `load_training_weight` + events training 分支 + raw/training 归档；见 [03](03-比赛与导入.md) §3.4）
3. 权重试算页 `/admin/rating`（P6：试算不落库不进缓存，「应用」写 YAML + bump + 审计）
4. Plotly 图表挂详情页
5. ~~赛年/赛季筛选接线~~ ✅ 已于 2026-09-14 提前完成（PeriodFilter 自动解析日期 + 周期下拉具体选项，见 [04](04-Rating与榜单.md) §5）

### 第五步 · 五期上线

1. **注册限制决策**（公网前必须定：邀请码或域名白名单）
2. 部署三件套：生产 rxconfig、`reflex export` + rsync、systemd、Caddyfile、备份 cron（清单见 [06-部署与运维](06-部署与运维.md) §7）

### 旁路 · 工程健壮性

- ~~测试盲区补齐~~ ✅ 2026-09-14：calendar（含周期解析）、audit、migrate（临时仓库端到端+幂等）、staged confirm/discard（core 层 xlsx 构造）、TrainingOi/Solo/OjContest/OjPractice/Dispatcher 计算器与未知类型报错路径；顺带修复 migrate 子函数无视 repo_root 参数的 bug
- ~~赛年/赛季筛选接线~~ ✅ 2026-09-14（原列于四期顺带）：PeriodFilter 模型层自动解析日期 + 周期下拉改为具体选项（数据覆盖范围生成）
- alembic 引入（四期动表结构前）
- formal 导入 DTO 前置校验（`total_teams`、`contest_type` 提前报错）

---

## 4. 待定决策清单（开放问题汇总）

> 以下为「决策未关」事项，与承诺功能分开跟踪；拍板后移入 §3 或移出范围。

| # | 事项 | 说明 | 出处 |
|---|------|------|------|
| 1 | **Rating 正式公式** | 当前 placeholder_v0，加权 Elo/衰减/封顶/初始分 | 04 §3 |
| 2 | **训练赛成绩分摊** | 均分还是按贡献；训练赛录入的前置 | 03 §3.4 |
| 3 | **OJ 数据源立项** | 适配器方案（爬虫/API/手工导入）+ 各 OJ Rating 归一化；三表已建零代码 | 03 §4 |
| 4 | **注册限制** | 邀请码 / 域名白名单，公网前必须定 | 06 §6 |
| 5 | 密码找回 UI | CLI 重置已有；是否做 admin UI | 05 §9 |
| 6 | `data/public/` 只读导出去留 | 原模块「可选降级」；无外部消费者则整体砍掉 | 原 05 文档 |
| 7 | `xcpc-data dump-json` | 脱库审阅用，建议保留 | 原 05/10 文档 |
| 8 | 队伍详情页 | 「二期再议」，未议 | 05 §9 |
| 9 | 上传文件大小上限 | 建议 10MB | 05 §9 |
| 10 | 是否展示「置信度」 | 参赛场次过少时标注 | 04 §3 |
| 11 | `retired` 绑定用户保留写权限 / `left` 自动解绑 | 建议状态 | 05 §9 |
| 12 | 是否保留「置信度」以外的榜单增强 | 分项榜（仅 ICPC/仅训练赛）、历史快照存档 | 04 §8 |

---

## 5. 里程碑历史明细（已完成部分）

### 一期 · 地基 ✅（2026-08-18）

- pyproject 统一打包（uv + Python 3.13）、SQLite 建表 + store 重构为 repository + `migrate` 幂等灌数据
- board 榜单聚合（竞赛排名、delta_recent、data_version 进程内缓存）
- Reflex 骨架 + `/` 榜单只读页（BoardState、筛选/排序/搜索）

### 二期 · 认证 ✅（2026-08-20）

- reflex-local-auth 0.5.0 接入；UserProfile / BindingRequest 以 web 层 SQLModel 存 `xcpc_web.db`（跨库 player FK 移除，service 层校验）
- AuthState（角色/绑定 computed var）+ `create_admin.py`
- `/login` `/register` `/profile`；权限三层守卫落地 `states/admin/base.py`
- `/admin` 概览 + `/admin/users` 绑定审批（unique 预检 + 自动驳回其余 pending）
- 审计日志 API；web 测试基建（conftest 状态链）

### 三期 · 管理后台 ✅（2026-09-10 关闭）

- P4a–P4d 四页 Web 集成 + 自动化测试 + 真实 xlsx staged→confirmed 验证（解析 312 队、6 条正式成绩、18 个未匹配选手）
- `create_admin.py --reset-password`、预备队员（probation）状态与入队操作
- 修复：核心 API 会话泄漏导致连接池耗尽
- 浏览器手工验收通过（admin 全程不敲 CLI 完成 CRUD/审批/导入五步），2026-09-10 关闭三期

### 三期后增量（随缺失评估推进）

- 榜单筛选同步 URL query（2026-09-11）
- `meta.data_version` 写路径自动 bump（2026-09-11）
- `/about` 静态页（2026-09-11）

---

*docs/ v2 重构（2026-09-11）：合并原 13-实施路线图 v1.6、14-Web开发拆分计划 v1.9、GAP_REPORT_PLAN。*
